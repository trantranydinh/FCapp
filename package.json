{
  "name": "fcapp-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npm install --prefix frontend && npm run build --prefix frontend && npm start --prefix backend",
    "dev": "echo 'Use npm start to run the single-entrypoint application' && exit 1"
  },
  "devDependencies": {
    "baseline-browser-mapping": "^2.9.19",
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "mssql": "^12.2.0",
    "tedious": "^18.6.1"
  }
}
